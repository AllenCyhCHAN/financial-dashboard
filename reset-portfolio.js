// Clear localStorage and reload with your actual portfolio data
// Run this in browser console to reset the app with your portfolio data

localStorage.clear();
console.log(
  'LocalStorage cleared. Your actual portfolio data will be loaded on next page refresh.'
);

// Optional: Force reload the page
// window.location.reload();
